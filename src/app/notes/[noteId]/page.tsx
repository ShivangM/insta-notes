// import NoteSection from "@/components/purchase note/NoteSection";
import { Note } from "@/domain/entities/Note";
import React from "react";

type Props = {
  params: {
    noteId: string;
  };
};

const page = ({ params: { noteId } }: Props) => {
  return <div></div>;
};
export default page;
